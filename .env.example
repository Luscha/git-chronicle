# Copy to `.env` and fill in. `.env` is git-ignored; this template is committed.
# Referenced from config.toml as api_key = "env:OPENAI_API_KEY".
# Vertex needs none of this — it authorises with Application Default Credentials.

OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# AZURE_OPENAI_KEY=
