# Copy to `.env` and fill in. `.env` is git-ignored; this template is committed.
# Referenced from config.toml via the `env:` prefix, e.g. api_key = "env:YOUR_API_KEY".

# Your chat-model provider API key (Scaleway, OpenAI, Together, Groq, ... — any OpenAI-compatible).
YOUR_API_KEY=
